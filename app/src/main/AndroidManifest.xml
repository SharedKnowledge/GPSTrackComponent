<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.gpstrackapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
    if target SDK Version is API Level 28 or higher
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme= "@style/NoActionBar"
        tools:replace="android:theme"
        >
        <activity android:name=".InitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrackRecordingMapActivity"/>
        <activity android:name=".DownloadTilesActivity"/>
        <activity android:name=".DisplayTracksActivity"/>
        <activity android:name=".MergeTracksActivity"/>
        <activity android:name=".SaveTracksActivity"/>
        <activity android:name=".DeleteTracksActivity"/>
        <activity android:name=".ImportTracksActivity"/>
        <activity android:name=".ExportTracksActivity"/>

        <service android:name=".location.LocationService"
            android:enabled="true"
            />
        <!--
            if target SDK Version is API Level 29 or higher add this to the Location Service
            android:foregroundServiceType="location"
        -->
    </application>

</manifest>